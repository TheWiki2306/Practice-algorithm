let array = [10, 20, 15, 30, 40];
let num = array.length;
let i = array.length - 1;

function heapify(array, num, i) {
    let largestElement = i;    
    let left = (2*i);
    let right = (2*i+1);
}

function heapSort() {
    
}